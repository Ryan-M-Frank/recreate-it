# DRAFT-REPLACE — Replace with a spoiler-free title

**AUTHORING TEMPLATE — not an official playable challenge.**
Challenge version: 0.1.0. Protocol version: 0.1.0. Mode: solo.

Replace every drafting prompt before publication. After review, replace this banner with PLAYER VIEW — safe to open and the actual release status.

## Problem and starting state

Describe the task and what initially exists. Avoid target algorithm names, mechanism tags, or hints disguised as story details.

## Inputs and operations

For each operation, state its inputs, effects, and exact result.

- Valid input domain:
- Initial state:
- Ordering, duplicates, and ties:
- Empty, absent, and boundary behavior:
- Invalid input behavior, or explicitly excluded inputs:
- Sequential or concurrent calls:

## Fixed acceptance constraints

Separate required behavior and resource limits from optional goals.

- Requirements and prohibited shortcuts:
- Allowed resources and computational model:
- Required time/space bounds, if any, with parameters:
- Tradeoffs to explain even when no bound is required:
- Explicitly out of scope:

Any design satisfying these rules counts. There is no requirement to resemble a hidden comparison solution.

## Stages

One fixed stage. If this draft needs more, list every stage's full requirements and trigger here before play. Optional extensions do not change success on the base contract.

## Public example

Give a small sequence of inputs and exact expected outputs. Demonstrate operation semantics without suggesting the representation.

## Starting prompt

Ask the player to describe what they would store and how one operation would behave. Code is optional unless explicitly required above.

The GM asks before hints. You may pause without revealing comparison material. Finishing or explicitly ending the round permits the reveal according to the selected mode.
